singapore_government_directory:
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: singapore-government-directory
      keyfile: "../extractor/token/gcp_token.json"
      dataset: dbt
      threads: 4
      timeout_seconds: 300
      location: EU

  target: dev
